{
  "name": "austinfay.com",
  "version": "1.0.0",
  "description": "The code for austinfay.com",
  "author": "Austin Fay",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "generate": "nuxt build && nuxt export",
    "clean": "rm -r dist .nuxt"
  },
  "dependencies": {
    "@nuxt/components": "^0.3.3",
    "@nuxt/content": "^1.2.0",
    "nuxt-edge": "^2.13.0-26517621.484e70fd"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "sass-loader": "^8.0.2"
  }
}
